'use strict';

describe('myApp.ingreso module', function() {

  beforeEach(module('myApp.ingreso'));

  describe('ingreso controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var IngresoCtrl = $controller('IngresoCtrl');
      expect(IngresoCtrl).toBeDefined();
    }));

  });
});