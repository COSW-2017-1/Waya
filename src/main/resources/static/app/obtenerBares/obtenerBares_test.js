'use strict';

describe('myApp.obtenerBares module', function() {

  beforeEach(module('myApp.obtenerBares'));

  describe('obtenerBares controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var obtenerBaresCtrl = $controller('obtenerBaresCtrl');
      expect(obtenerBaresCtrl).toBeDefined();
    }));

  });
});